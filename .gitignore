# === Dependencies ===
# Lockfiles (usually committed, but ignoring here for completeness if desired by team)
# package-lock.json
# yarn.lock
# pnpm-lock.yaml

# Dependency directories (NEVER commit these)
/node_modules

# === Next.js ===
# Build output
/.next/
# Static HTML export output
/out/

# === Environment Variables ===
# Exclude all .env files containing local secrets and config
# Only commit .env.example (if you create one)
.env
.env*.local
# Do NOT add .env.production to this list if you plan to commit it
# (though managing production secrets differently is recommended)

# === Logs ===
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lcov.info
*.lcov

# === Runtime data ===
pids
*.pid
*.seed
*.pid.lock

# === Build & Cache ===
*.tsbuildinfo
.eslintcache
.stylelintcache
.next.lock
# TurboRepo cache (if using turborepo)
.turbo

# === Test Coverage ===
coverage

# === OS generated files ===
# Mac
.DS_Store
._*
.Spotlight-V100
.Trashes
# Windows
Thumbs.db
ehthumbs.db
Desktop.ini
# Linux
*~

# === IDE / Editor directories (Optional - uncomment if you don't want them committed) ===
# .vscode/
# .idea/
# *.sublime-workspace
# .history/